<div class="container">
  <%= render "/shop/shared/paid_ranking", users: @users, url: "/shop/products/#{@task.id}/more_users" %>
  <div class="text-center">
    <%= will_paginate(@users) %>
  </div>
</div>
